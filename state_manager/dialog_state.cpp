#include "dialog_state.h"
#include "state_manager.h"

DialogState::DialogState(StateManager *manager)
  : State(manager) {
}

DialogState::~DialogState() {
}

void DialogState::do_cmd(Cmd &cmd) {
}

void DialogState::do_action(Cmd &cmd) {

void DialogState::do_message(Cmd &cmd) {
}

